[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
pythonpath = .
addopts =
    --tb=short
    --strict-markers
    --disable-warnings
    --color=yes
markers =
    memory: Memory Manager tests
    document: Document Vault tests
    integration: Integration tests
